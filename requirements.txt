# Microsoft Agent Framework (Python) -- mandatory per the brief.
# The orchestrations package is a separate install and provides
# MagenticBuilder; without it the orchestrator will not import.
agent-framework==1.14.0
agent-framework-orchestrations==1.1.0
agent-framework-gemini==1.0.0b260813

# LLM provider
google-genai==2.18.1

# Data contracts for the request, recommendation and evaluation, and for
# the persisted memory and checkpoint files.
pydantic

# RAG over SEC filings: local vector store plus the embedding model.
chromadb
sentence-transformers
langchain-text-splitters

# Filing download and parsing (SEC EDGAR), and the news API.
requests
beautifulsoup4

# Market data, served through the MCP server in src/mcp/market_server.py
yfinance
mcp

# Agent-to-Agent protocol: exposes the Risk Analyst as a service that
# other runtimes can call. Only needed for src/a2a_server.py and
# tests/test_a2a.py; the main pipeline runs without them.
agent-framework-a2a==1.0.0b260730
a2a-sdk==1.1.2
starlette
uvicorn
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-grpc==1.44.0
python-dotenv
streamlit